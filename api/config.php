<?php

/**
 * 🌿 昆虫与环保网站 - 数据库配置文件
 * 
 * ⚠️ 部署时必须修改此文件！
 * 
 * 📍 需要修改的地方（已用 👈 标注）：
 * 1. MySQL 数据库连接信息（主机、端口、数据库名、用户名、密码）
 * 2. 后台管理员登录密码
 */

return [
    // ============================================
    // 🔗 MySQL 数据库连接配置
    // ============================================
    
    'database' => [
        'type'      => 'mysql',           // 数据库类型：mysql
        'host'      => 'localhost',       // 👈 数据库主机地址（宝塔面板通常是 localhost 或 127.0.0.1）
        'port'      => '3306',            // 👈 数据库端口（默认3306，如果不同请修改）
        'database'  => 'insect_eco',      // 👈 数据库名称（需要在MySQL中先创建这个数据库）
        'username'  => 'root',            // 👈 数据库用户名（宝塔面板默认是 root）
        'password'  => 'your_password',   // 👈 数据库密码（⚠️ 必须修改为你的真实密码！）
        'charset'   => 'utf8mb4',         // 字符集（支持emoji等特殊字符）
        'collation' => 'utf8mb4_unicode_ci',
        
        // 连接选项（一般不需要修改）
        'options'   => [
            PDO::ATTR_ERRMODE            => PDO::ERRMODE_EXCEPTION,
            PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
            PDO::ATTR_EMULATE_PREPARES    => false,
        ],
    ],

    // ============================================
    // 🔐 后台管理员账户配置
    // ============================================
    
    'admin' => [
        // 管理员用户名（一般不需要改）
        'username' => 'admin',
        
        // 👈 管理员登录密码（⚠️ 首次部署时必须修改！）
        // 这个密码会保存到数据库中，之后可以通过后台界面修改
        'password' => 'admin123',
        
        // 提示：建议使用强密码（字母+数字+特殊字符，长度8位以上）
        // 示例：'MyP@ssw0rd2024!'
    ],

    // ============================================
    // 🌐 站点基本配置
    // ============================================
    
    'site' => [
        'name'    => '昆虫与环保',
        'version' => '2.0-PHP-MySQL',
    ],

    // ============================================
    // 🔒 安全配置
    // ============================================
    
    'security' => [
        // 允许的API请求来源（* 表示允许所有）
        'allowed_origins' => ['*'],
        
        // 是否启用XSS防护
        'enable_xss_protection' => true,
    ],
];
